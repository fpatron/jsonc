version: 2

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
- id: betterjson
  main: ./example/main.go
  env:
    - CGO_ENABLED=0
  goos:
    - linux
    - windows
    - darwin

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
